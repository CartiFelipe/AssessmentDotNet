@page
@using System.Globalization
@model IndexModel

@{
    ViewData["Title"] = "Index";
    Layout = "~/Pages/Shared/_Layout.cshtml";
}

<h1>Pacotes turísticos</h1>

<p>
    <a asp-page="Create">Criar nova</a>
</p>
<table class="table">
    <thead>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TouristPackage[0].Title)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TouristPackage[0].InitialDate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TouristPackage[0].MaximumPeople)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TouristPackage[0].Price)
        </th>
        <th></th>
    </tr>
    </thead>
    <tbody>
    @foreach (var item in Model.TouristPackage)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Title)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.InitialDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MaximumPeople)
            </td>
            <td>
                @string.Format(CultureInfo.CurrentCulture, "{0:C}", item.Price)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Editar </a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Detalhes </a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Deletar </a>
            </td>
        </tr>
    }
    </tbody>
</table>
