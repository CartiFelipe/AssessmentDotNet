@page
@model CreateModel

@{
    ViewData["Title"] = "Create";
    Layout = "~/Pages/Shared/_Layout.cshtml";
    <partial name="_ValidationScriptsPartial"/>
}

<h1>Criar</h1>

<h4>Reserva</h4>
<hr/>
<div class="row">
    <div class="col-md-4">
        <form method="post">

            <div asp-validation-summary="All" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Reservation.ClientId" class="control-label">

                </label>
                <select asp-for="Reservation.ClientId" class="form-control" asp-items="ViewBag.ClientId">
                    <option disabled>Selecione um cliente</option>
                </select>
            </div>
            <div class="form-group">
                <label asp-for="Reservation.TouristPackageId" class="control-label">
                    Pacote Turístico
                </label>
                <select asp-for="Reservation.TouristPackageId" class="form-control"
                        asp-items="ViewBag.TouristPackageId">
                    <option disabled>Selecione um pacote</option>
                </select>
            </div>
            <div class="form-group">
                <label asp-for="Reservation.ReservationDate" class="control-label"></label>
                <input asp-for="Reservation.ReservationDate" class="form-control" value="null">
            </div>
            <div class="form-group">
                <input type="submit" value="Criar" class="btn btn-primary"/>
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="Index">Voltar para a lista</a>
</div>

